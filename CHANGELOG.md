# Изменения

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/).

## [0.4.1] — 2026-08-01

### Добавлено

* **База по Ubuntu/Linux расширена: команды, приёмы, «секреты».**
  Два новых урока — «Ubuntu в консоли: команды, приёмы и маленькие секреты»
  (DevOps, linux-basics) и «Приёмы консоли для поддержки: журналы, архивы,
  история» (Technical Engineer, ts-linux). 10 терминов глоссария (apt, sudo,
  cron, конвейер, grep, find, tar, ss, df/du, stdout/stderr) — все видны обоим
  трекам. 11 вопросов теста с разборами (6 DevOps + 5 поддержка): окружение
  cron, права 754, удалённый открытый файл и df, ss -tulpn, 2>&1,
  update/upgrade, journalctl --since, kill против kill -9, grep -C,
  tar -tzf, Ctrl+R. В библиотеке 4 новых материала: Ubuntu Server docs,
  вводный курс Canonical по командной строке, tldr pages и explainshell —
  ссылки проверены живьём 2026-08-01. Уроки, вопросы и термины связаны
  перекрёстными ссылками.

## [0.4.0] — 2026-08-01

Закрытие находок полного аудита (три независимых прохода: код, безопасность,
тесты/деплой).

### Исправлено

* **«Сбросить прогресс» удаляет данные и на сервере.** Кнопка чистила только
  устройство, и следующая синхронизация возвращала всё обратно — вопреки
  обещанию PRIVACY.md. Теперь сброс сначала зовёт `DELETE /api/me` и честно
  останавливается, если сервер недоступен (`Sync.wipe`).
* **Слияние не воскрешает мёртвую серию**: streak берётся у источника с более
  свежим `lastDay` (как страховка серии), а не по максимуму. Рекорд серии
  (best) по-прежнему сохраняется.
* **Честное понижение самооценки больше не откатывается**: оценка mock, кейса
  и задания английского берётся у более свежей записи по `ts`, как при
  локальной перезаписи, а не по максимуму. Счётчик заходов — максимум.
* **Новое устройство видит серверный прогресс**: при пустом localStorage
  главная сначала спрашивает сервер (до 4 секунд) и лишь потом отправляет в
  онбординг.
* **Результат поиска по вопросу открывает сам вопрос**: quiz.html понимает
  `?id=…` (режим одного вопроса) — раньше ссылка вела в общее меню тестов.
* **Достижения не «отзываются»**: полученное достижение фиксируется флагом
  (сливается по ИЛИ) и не гаснет, когда идеальный тест вытесняется из
  истории, обрезанной до 100 записей.
* **Бот знает трек пользователя**: случайные вопросы и mock приходят из
  выбранного трека (команда `/track`, выбор запоминается в progress/),
  названия категорий mock стали нейтральными к профессии.

### Безопасность

* Тело запроса читается потоком с потолком, а не буферизуется целиком до
  проверки лимита; в Caddy добавлен `request_body max_size 1MB` на `/api/*`.
* Потолок суммарного размера состояния пользователя (1 МБ): серия запросов
  больше не может наращивать запись до заполнения диска — запись отвергается,
  прежнее состояние не страдает.
* initData без `auth_date` отвергается: подписанная строка без метки времени
  жила бы вечно.
* Подняты зависимости: fastapi 0.141.1 (starlette 1.3.1 — закрыты 9 известных
  advisories транзитивного starlette 0.41), uvicorn 0.52, pydantic 2.13.4,
  aiogram 3.30. `pip-audit` добавлен в CI и чист.

### Инфраструктура и проверки

* **Деплой-документация приведена к реальности**: на сервере нет git — код
  уезжает архивом. Новый `deploy/release.sh` (с рабочей машины) делает архив,
  удаляет файлы, исчезнувшие из репозитория (tar сам не удаляет), и запускает
  серверный `deploy/update.sh` (без git-шагов). `update_webapp.sh` удалён.
* **Смоук строже**: `/api/*` проверяется и через reverse-proxy, заголовки
  кэша (включая `no-store` у service worker), POST/DELETE без initData,
  живость бот-сервиса, свежесть бэкапа (порог 48 ч); наполненность трека
  определяется по статусу из tracks.json, а не порогом в 200 КБ.
* **Пер-трековые минимумы контента** в валидаторе: пустой пятый трек больше
  не спрячется за суммарным объёмом банка.
* **Страж whitelist профиля**: каждый ключ, который приложение пишет в
  профиль, обязан пережить слияние на обеих сторонах (статический скан +
  фикстура полного профиля в тесте паритета); число фикстур паритета
  сверяется — `zip` больше не может молча усечь новые случаи.
* Списки тестов и проверок в CI, run_all_checks.sh и update.sh собираются по
  каталогу, а не руками; в CI добавлен офлайн-аудит источников.

## [0.3.1] — 2026-08-01

### Добавлено

* **База шпаргалок OverAPI.com в библиотеке** — новый тип материала
  «шпаргалка» (`cheatsheet`), 10 страниц с реальным встроенным контентом:
  Linux, Git, Docker, MySQL, регулярные выражения, Python, HTML, CSS,
  JavaScript, mod_rewrite. Каждая привязана к темам своих треков (от
  `linux-basics` до `technical-seo`). Из 43 релевантных страниц каталога
  отобраны только содержательные: остальные — пустые оболочки с одной
  ссылкой на Википедию, в базу не попали.
* **Вкладка «Ресурсы» в разделе «Английский для IT»** — маршрут регулярной
  практики из 9 проверенных ресурсов с методикой «как использовать»:
  курсы Cisco English for IT 1–2 (последовательно), два конспекта-курса с
  GitHub, Speak Tech English (рассказ о своей работе), MDN и GitHub Skills
  (чтение и задания только на английском), YouGlish (произношение каждого
  нового термина), Cambridge Write & Improve (описания своих проектов).
  Новый файл `data/english_resources.json`; у ресурсов нет состояния —
  профиль и правила слияния не затронуты.
* Все 19 новых ссылок добавлены в `sources.json` и проверены живьём
  2026-08-01 (для netacad проверялся заголовок страницы: их SPA отвечает
  200 на любой адрес).

## [0.3.0] — 2026-07-28

### Добавлено

* **Четвёртый трек — Technical Engineer** (инженер технической поддержки).
  20 тем (8 критических), 20 уроков, 124 вопроса теста, 33 вопроса mock
  interview по всем категориям сессии, 8 кейсов, план из 22 шагов, обобщённая
  вакансия на 18 требований, 6 шаблонов историй, 76 терминов словаря
  (20 новых плюс расширенные на трек существующие), 16 новых материалов
  библиотеки, 8 своих слов и 4 задания в разделе английского.
* Закрыты все пробелы отчёта о покрытии: материалы библиотеки для тем
  internal-linking, dev-communication, interview-seo, cloud-basics и
  поведенческих тем всех треков, плюс 9 терминов в новой категории словаря
  «Работа в команде». Аудит источников: 197 из 197 доступны.

### Исправлено

* `_readiness_check` требовал, чтобы в контенте существовал незаполненный трек.
  После наполнения четвёртого такого трека не осталось, и проверка падала.
  Правило «незаполненный трек не получает процента» теперь проверяется на
  синтетическом треке: проверяется поведение расчёта, а не состав данных.
* `deploy/smoke.sh` перечислял страницы Mini App вручную — новый экран
  выкатывался, а смоук рапортовал «всё пройдено», не запросив его. Список
  страниц берётся из каталога `webapp/`.

## [0.2.0] — 2026-07-28

### Добавлено

* **Раздел «Английский для IT»** — сквозной, виден на всех треках: плашка на
  главной, экран `english.html` из четырёх вкладок и кнопка в меню бота.
  * *Фразы* — 64 оборота собеседования по восьми ситуациям: рассказ о себе,
    опыт, технические объяснения, «переспросить и выиграть время», что делать,
    когда не знаешь ответа, вопросы работодателю, условия и финал.
  * *Слова* — 68 терминов, которые чаще всего произносят неверно: транскрипция,
    подсказка русскими буквами, типичная ошибка, пример и ссылка на словарную
    статью. Есть режим карточек и интервальное повторение.
  * *Тренажёр* — 19 заданий «ответ вслух»: вопрос, таймер, эталон и рубрика
    самооценки 0–4, как в mock interview.
  * *Письмо* — 40 конструкций документации и шаблонов переписки: баг-репорт,
    письмо после собеседования, статус инцидента, код-ревью, созвоны.
  * Общая часть одинакова для всех, лексика и задания профессии — свои: SEO,
    DevOps и QA-платежи видят свои слова и свои ситуации.
* Английский участвует в дневной цели и серии, но **не входит** в расчёт
  готовности к профессии: процент отражает знание предметной области.
* Поиск находит фразы, слова, задания и шаблоны наравне с остальным контентом;
  в фильтрах поиска появилась кнопка «Английский».

### Исправлено

* **Тихая потеря прогресса при синхронизации.** Разделы профиля `lessons`
  (прочитанные уроки), `quizBest` (лучший результат по теме) и
  `weakTopicsSeen` не входили в белый список правил слияния, а клиент заменяет
  профиль целиком ответом сервера. После первого же обмена отметки уроков и
  лучшие результаты обнулялись, а шаги плана с условиями `lesson_read` и
  `quiz_score_min` откатывались в невыполненные. Правила добавлены на обе
  стороны и закрыты тестом паритета.

### Проверки

* Валидатор: схемы четырёх новых коллекций, запрет кириллицы в английских
  полях, формат транскрипции и словарной ссылки.
* Потрековые проверки: каждый активный трек обязан видеть минимум записей в
  каждом разделе английского и иметь свои, не сквозные, слова и задания.
* Поиск: структурная проверка типов расширена на английский и идёт по всем
  активным трекам.

## [0.1.0] — 2026-07-27

Первая версия MVP.

### Продукт

* Telegram-бот как launcher Mini App с командами `/start`, `/menu`, `/help`,
  `/stats`, `/privacy`. Упрощённый режим прямо в чате, когда Mini App не
  подключён: случайные вопросы с разбором, работа над ошибками, вопрос для
  mock interview, статистика.
* Mini App из 16 экранов: главная, настройка, план подготовки, теория, урок,
  практика, mock interview, кейсы, словарь, библиотека, вакансия, истории,
  профиль, история, поиск, о приложении.
* Онбординг: выбор трека, самооценка по 11 областям, дата собеседования,
  дневная цель.
* План подготовки из 24 шагов с зависимостями и проверяемыми условиями
  завершения.
* Интервальное повторение (система Лейтнера, 6 коробок) для вопросов,
  терминов, открытых вопросов и кейсов.
* Семь режимов практики: быстрый тест, тренировка, финальный экзамен,
  повторение, работа над ошибками, слабые темы, по темам.
* Mock interview с рубрикой самооценки и режимом полной сессии по блокам
  настоящего собеседования.
* Практические кейсы в два этапа: сначала своя попытка, затем эталонный разбор.
* Экран вакансии с gap-анализом: статусы требований, доказательства из
  портфолио, пробелы, следующие шаги.
* Story bank: 12 шаблонов STAR.
* Единый поиск по урокам, терминам, материалам, вопросам, кейсам и шагам плана
  с поддержкой русского, английского и синонимов.
* Достижения, карта активности, история с графиком готовности.

### Расчёт готовности

* Пять взвешенных блоков: знания и SRS 35%, mock interview 25%, кейсы 20%,
  словарь 10%, план и регулярность 10%.
* Четыре ограничителя: нет полной сессии mock — потолок 74%, меньше трёх
  кейсов — 79%, слабая критическая тема — 69%, низкое покрытие обязательных
  тем — 64%.
* Самооценка из настройки не участвует в расчёте.
* Незаполненный трек не получает процента готовности.
* Расчёт чистый и воспроизводимый: одинаковый вход даёт одинаковый выход.
* План к дате учитывает работу всех типов, а не только число тестов.

### Контент

Трек `redcore-junior-seo` (RedCore, Junior SEO Specialist):

* 20 тем, из них 6 критических;
* 28 уроков;
* 111 терминов словаря в 15 категориях;
* 62 материала библиотеки;
* 120 вопросов теста с разбором каждого варианта;
* 46 открытых вопросов mock interview;
* 12 практических кейсов;
* 24 шага плана подготовки;
* 12 шаблонов историй STAR;
* 24 достижения;
* 63 источника, все URL проверены HTTP-запросом 27.07.2026.

Заявлены как `coming_soon`, без контента и без расчёта готовности:
`qa-payments`, `technical-engineer`, `devops-platform`.

### Инфраструктура

* Sync API на FastAPI и SQLite: авторизация по HMAC-подписи Telegram
  `initData`, слияние без потерь, атомарный read-merge-write, ограничение
  частоты и размера запроса, удаление данных пользователя.
* Правила слияния продублированы в клиенте и на сервере с обязательным тестом
  паритета.
* Service worker: network-first для кода и страниц, stale-while-revalidate для
  статики, `/api/*` мимо кэша.
* Инструменты: валидация контента (более 2300 проверок), генератор данных
  приложения с проверкой свежести, аудит источников по сети, поиск дублей,
  отчёт о покрытии.
* CI: три задачи — Python, JavaScript, гигиена репозитория. Секрет-скан,
  проверка отсутствия `.env` и пользовательских данных, проверка на следы
  референсного проекта.
* Развёртывание: systemd-юниты с песочницей, пример конфигурации Caddy,
  скрипты обновления, бэкапа с проверкой восстановления и проверки живости.

### Тесты

* Python: контент (22), слияние и паритет (24), API (36), бот (30),
  безопасность (20), нагрузка (2) — 134 теста.
* JavaScript: SRS, прогресс, готовность, слияние, поиск, service worker,
  навигация — 7 наборов проверок без npm-зависимостей.

### Известные ограничения

* Свободный текст ответа не оценивается: без настоящей языковой модели такая
  оценка была бы имитацией. Вместо этого разбор, эталон и рубрика самооценки.
* Наполнен один трек из четырёх.
* Один язык интерфейса.
* Сервер доверяет присланному прогрессу: серверной проверки нет.
* Бэкапы хранятся на той же машине.
